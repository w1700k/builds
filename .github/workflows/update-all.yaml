name: update-all

on:
  schedule:
    - cron: '15 */22 * * *'
  workflow_dispatch:

env:
  REMOTE_REPOSITORY: joelinux60/openwrt-joelinux60

jobs:
  update:
    name: Update ${{ matrix.environment }} branch
    runs-on: ubuntu-slim
    strategy:
      matrix:
     #   environment: [main, snapshot, test, tiger, ubi2-oc-auto]
        environment: [main]
    permissions:
      contents: write
    environment: ${{ matrix.environment }}
    steps:
      - name: Checkout remote repository
        uses: actions/checkout@main
        with:
          repository: ${{ env.REMOTE_REPOSITORY }}
          ref: ${{ matrix.environment }}
          fetch-depth: 0
          token: ${{ secrets.REPO_SECRET }}
      - name: Pull updates then push
        run: |
          git config user.name github-actions[bot]
          git config user.email github-actions[bot]@users.noreply.github.com
          git remote add openwrt https://github.com/openwrt/openwrt.git
          git remote update
          git rebase openwrt/main
          git push --force-with-lease
